apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: hello
  name: 12nginx-helloworld
  labels:
    app: nginx-rancherhello
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-ra213ncherhello
  template:
    metadata:
      labels:
        app: nginx-rancherhello
    spec:
      containers:
      - name: nginxhelloasdadsa
        image: nginx
        ports:
        - containerPort: 80
